<div>
    {{#each users}}
        <p><strong>Nombre:</strong> {{this.first_name}} {{this.email}}</p>
    {{/each}}
    {{#if hasPrevPage}}
        <a href="/api/usuarios?page={{prevPage}}"><button>ANTERIOR</button></a>
    {{/if}}
    {{#if hasNextPage}}
        <a href="/api/usuarios?page={{nextPage}}"><button>SIGUIENTE</button></a>
    {{/if}}
</div>